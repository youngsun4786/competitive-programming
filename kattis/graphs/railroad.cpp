#define push_back pb
#include<vector>
#include<iostream>

using namespace std;

int main() {

    int X, Y; cin >> X >> Y;

    (Y % 2 == 0) ? cout << "possible" : cout << "impossible"; 

    return 0;
}